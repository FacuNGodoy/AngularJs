import { FiltroClasePipe } from './filtro-clase.pipe';

describe('FiltroClasePipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroClasePipe();
    expect(pipe).toBeTruthy();
  });
});
